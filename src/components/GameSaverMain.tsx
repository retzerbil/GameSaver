import { RouterProvider } from "react-router-dom"
import { Router } from "../Router"

export const GameSaverMain = () => {
    return (
        <>
            <RouterProvider router={Router}></RouterProvider>
        </>
    )
}